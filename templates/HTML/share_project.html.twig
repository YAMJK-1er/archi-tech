<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="{{ asset('css/CSS/import_projet.css') }}">
    <link rel="stylesheet" href="{{ asset('css/CSS/fontawesome/css/all.min.css') }}">
    <title>{{ projet.nom }}</title>
</head>
<body>
    <div id="header">
        <div id="logo">
            Archi - Tech
        </div>

        <a href="/profil" style="text-decoration:none;color:white;">
            <div id="infos-profil-icons">
                <i class="fa fa-user-circle fa-2x" id="icons"></i>
                <span id="nom">({{user.prenom}} {{user.nom}})</span>
            </div>
        </a>
    </div>

    <div id="section">
        <div id="titre">
            Partage de projet
        </div>

        <hr id="hr-titre">

        {{ form_start(form) }}
            <div class="champ">
                <div class="intitule">Nom du projet</div>
                {{ form_row(form.nom) }}
            </div>
            
            <div class="champ">
                <div class="intitule">Code du projet</div>
                {{ form_row(form.code) }}
            </div>

            <div class="champ">{{ form_row(form.email) }}</div>

            
            {% if message %}
                <p id='error'> {{ message }} </p> 
            {% endif %}

            <div id='options'>
                <button type='submit'>Partager</button>
                <button id='cancel'><a href="/projets/{{ projet.id }}/details">Annuler</a></button>
            </div>
        {{ form_end(form) }}

        <div id="footer"><p><i class="fa fa-copyright"></i> Tout droits réservés , Oracle Logic Consulting SARL 2022</p></div>
    </div>

    <script src="{{ asset('js/jquery-3.6.0.min.js') }}"></script>
</body>
</html>